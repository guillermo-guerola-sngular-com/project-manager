# Frontend

Next.js 16 (App Router) + React 19 + TypeScript, built as a static export (`output: "export"`) and served by the FastAPI backend. The Kanban board and login are both fully backend-backed now — no in-memory demo data left; everything round-trips through `/api/*`. An AI chat sidebar sits alongside the board and can change it via `/api/ai/chat`.

## Structure

- `src/app/layout.tsx` — root layout; loads Space Grotesk (display font) and Manrope (body font) via `next/font/google`, sets page metadata.
- `src/app/page.tsx` — `/` route, renders `AppShell`.
- `src/app/globals.css` — Tailwind v4 import plus the app's CSS custom properties (colors from AGENTS.md's Color Scheme, surface/stroke/shadow tokens).
- `src/lib/kanban.ts` — domain types (`Card`, `Column`, `BoardData`; ids are `number`, matching the backend's integer primary keys) and the pure `moveCard` helper (reorder/move a card by id between/within columns, operating on `Column[]`). No I/O — this is the same reducer logic used for the optimistic local update on drag.
- `src/lib/dndCollision.ts` — `createCollisionDetectionStrategy(columns)`, the custom dnd-kit `CollisionDetection` factory extracted out of `KanbanBoard.tsx` so it's a pure function testable without a real drag (see `dndCollision.test.ts`). Refines a `pointerWithin`/`rectIntersection` hit on a column into whichever of that column's cards is actually closest via `closestCenter`, excluding the actively-dragged card from that refinement (see the in-file comments for why both steps are necessary).
- `src/lib/api.ts` — `fetchBoard` (fetches `GET /api/board` and flattens the nested `{columns: [{..., cards: [...]}]}` response into the normalized `BoardData` shape `lib/kanban.ts` and the dnd-kit-driven UI expect), `renameColumn`, `createCard`, `updateCard` (edit and/or move — sends `column_id`/`position`), `deleteCard`, `sendChatMessage` (posts `{message, history}` to `POST /api/ai/chat`, returns `{reply, boardChanged}`).
- `src/lib/auth.ts` — `fetchSession`/`login`/`logout`, thin wrappers around `fetch` calls to `/api/auth/*`. No token handling on the client — the JWT lives in an httpOnly cookie set by the backend.
- `src/components/AppShell.tsx` — top-level client component. On mount, calls `fetchSession()` to decide whether to render `LoginForm` or the board; owns `handleLogout` (passed down as `KanbanBoard`'s `onLogout` prop — rendered inside the board's own compact header rather than a separate bar, to keep the chrome above the columns to one thin row) and a `refreshSignal` counter passed to `KanbanBoard`, bumped by `ChatSidebar`'s `onBoardChanged` callback whenever the AI reports it changed the board. Renders `KanbanBoard` and `ChatSidebar` side by side once authenticated; renders only `LoginForm` (no sidebar) otherwise.
- `src/components/LoginForm.tsx` — username/password form, calls `lib/auth.login`, shows an error on failure.
- `src/components/KanbanBoard.tsx` — client component. Fetches the board from the API on mount (shows "Loading board…" until it resolves). Every interaction (rename, add, delete) updates local state immediately (optimistic) and fires the matching `lib/api.ts` call in the background; each handler clears the previous error first (`setError(null)`), so a failed call surfaces a banner that the next action — success or not — dismisses, rather than rolling back. Drag-and-drop is the exception: `board` is only mutated once, in `onDragEnd`, via `moveCard`; `onDragOver` only updates a separate `overColumnId` state used purely for the column highlight. Don't merge these back into one "mutate `board` on every drag event" — that was tried and made cross-container drags actively unstable. The collision-detection strategy itself lives in `lib/dndCollision.ts`, not here — `KanbanBoard.tsx` just calls `createCollisionDetectionStrategy(board?.columns ?? [])`.
- `src/components/KanbanColumn.tsx` — one column: droppable zone, editable title input, sortable list of cards, empty-state placeholder, renders `NewCardForm`.
- `src/components/KanbanCard.tsx` — one sortable/draggable card. The title/details area is a button that opens `CardFormModal` in edit mode; a small trash-icon button opens `ConfirmModal` before actually deleting. Nesting these inside the dnd-kit sortable `<article>` works because `PointerSensor`'s `distance` activation constraint lets plain clicks (no movement) through to inner buttons instead of starting a drag. The title button has `min-w-0` (plus `break-words` on the `h4` inside) — without it, a flex child's default `min-width: auto` sizes it to its content's intrinsic width, so on a narrow column a long title would force the header row wider than the card and push the trash-icon button out past the card's right edge instead of wrapping the title text.
- `src/components/KanbanCardPreview.tsx` — static (non-interactive) card render used inside `DragOverlay` while dragging.
- `src/components/NewCardForm.tsx` — "Add a card" trigger button that opens `CardFormModal` in create mode.
- `src/components/CardFormModal.tsx` — the title/details form, shared by card creation (`NewCardForm`) and editing (`KanbanCard`); which one it is depends only on whether `initialTitle`/`initialDetails` and the heading/submit-label props are set by the caller.
- `src/components/ConfirmModal.tsx` — generic confirm/cancel dialog; currently used for the delete-card confirmation.
- `src/components/Modal.tsx` — the shared overlay/dialog shell (`position: fixed` backdrop, close on backdrop click or Escape, `role="dialog"`) that `CardFormModal` and `ConfirmModal` are built on. Add new popups on top of this rather than hand-rolling another overlay.
- `src/components/ChatSidebar.tsx` — the AI chat panel: message list (`role: "user" | "assistant"` bubbles) plus a text input and send button. On submit, sends the message and the prior turns (as `history`, not yet including the new message) to `lib/api.ts`'s `sendChatMessage`, appends the reply, and calls the `onBoardChanged` prop iff the response said `boardChanged`. Doesn't touch `board` state itself or duplicate any operation-parsing logic — that all lives server-side (`backend/app/ai.py`/`app/chat.py`); this component only reacts to the boolean flag. Collapsible: a local `collapsed` boolean (not persisted, not a prop — `AppShell` doesn't need to know) swaps the full 360px panel for a 56px tab with just a reopen button, so it can be tucked away without losing conversation state (the `messages` array is untouched by collapsing).

## Auth model

Login is enforced client-side: `AppShell` always renders the same static HTML/JS (it's a static export, there's no server-rendered gate), and on mount asks the backend "am I logged in?" via `GET /api/auth/me`. Unauthenticated → `LoginForm`; authenticated → the board plus a logout button. The actual security boundary is server-side: the JWT cookie is httpOnly (unreadable from JS), and every board route requires it via the backend's `get_current_user` dependency. Don't rely on this client-side gate alone to protect data — it's UI routing, not the auth boundary.

## State model

Board state is a single `BoardData` object: `columns` (ordered array with `cardIds`) plus `cards` (id-keyed map), populated from `fetchBoard()` on mount. All mutations in `KanbanBoard.tsx` are immutable local updates via `setBoard`, paired with a background API call. `moveCard` in `lib/kanban.ts` is the only place drag-and-drop reordering logic lives — reuse it rather than re-deriving column/card index math elsewhere. Card/column ids are whatever the database assigned; there's no client-side id generation anymore.

`KanbanBoard` also accepts an optional `refreshSignal: number` prop (default `0`) in its `fetchBoard()` effect's dependency array — bumping it forces a full refetch. This is how the AI chat sidebar's board changes reach the board: rather than have the frontend parse/replay the AI's operations (which would duplicate the server-side operation-application logic in `backend/app/chat.py`), `AppShell` just increments a counter and lets `KanbanBoard` re-pull the authoritative state. Simpler, but means a brief "everything re-renders from scratch" moment instead of a targeted patch — acceptable for an MVP where that fetch is local and fast.

## Styling

Tailwind v4, configured via `@theme inline` in `globals.css` (no `tailwind.config.*` file). Colors are referenced as CSS vars (`var(--navy-dark)`, etc.), not Tailwind color utilities — keep using the vars for consistency when adding UI.

## Testing

- Unit/component tests: Vitest + Testing Library + jsdom. Files live next to source as `*.test.ts(x)` (see `src/lib/kanban.test.ts`, `src/lib/dndCollision.test.ts`, `src/components/KanbanBoard.test.tsx`, `src/components/LoginForm.test.tsx`, `src/components/AppShell.test.tsx`, `src/components/ChatSidebar.test.tsx`). Board/auth/chat tests mock `global.fetch` via `vi.stubGlobal`, routing on `(url, options.method)` — see `createFetchMock` in `KanbanBoard.test.tsx` for the pattern. `ChatSidebar.test.tsx` covers: a reply renders, `onBoardChanged` fires only when the response says `board_changed: true`, prior turns are included as `history` on the next send, and a failed request shows an alert. `dndCollision.test.ts` builds fake dnd-kit `CollisionDetection` args by hand (`ClientRect`/`DroppableContainer`/`Active` fixtures) to exercise the refinement logic directly — no real drag needed: one case checks a drop in the gap between two cards resolves to the nearest card rather than the column, the other checks the actively-dragged card is excluded from its own column's refinement even when its rect is geometrically closest. Config: `vitest.config.ts`, setup in `src/test/setup.ts`. Run with `npm run test:unit`.
- E2E: Playwright, tests in `tests/*.spec.ts` (`tests/kanban.spec.ts`, `tests/auth.spec.ts`, `tests/chat.spec.ts`; shared login helper in `tests/utils.ts`). Since login and the board are both server-backed, `playwright.config.ts`'s `webServer` builds and runs the real Docker image (`docker build .. && docker run ... -p 3000:8000`) rather than a frontend-only `next dev` server. `kanban.spec.ts` locates columns/cards by their rendered title/testid rather than hardcoded ids, since the database assigns its own ids independent of the old frontend demo's `col-backlog`-style ones. `chat.spec.ts` sends real chat instructions through to the real OpenRouter model (no mocking, same as the backend's real-call tests) and asserts the resulting card shows up scoped inside the target column — not just anywhere on the page, since the sidebar's own reply text can contain the same card name as a substring and would otherwise give a false pass. Run with `npm run test:e2e` (requires Docker running; downloads browsers once via `npx playwright install chromium`).
- `npm run test:all` runs both.
- Gotcha when querying by role+name for anything inside `KanbanCard`: dnd-kit gives the whole card's `<article>` `role="button"` too (for keyboard drag support), and its computed accessible name concatenates all descendant text/labels — so a fuzzy `getByRole("button", { name: /delete .../i })` will match both that article and the actual delete/edit button. Use `{ name: "...", exact: true }` (or the specific accessible name) to disambiguate, as `KanbanBoard.test.tsx` and `tests/kanban.spec.ts` do.

## Known gaps

- Same-column card reordering via drag is implemented (same `moveCard` codepath as cross-column moves, which e2e does cover) but hasn't been confirmed with a real mouse drag: Playwright's synthetic drag for this specific case didn't reliably trigger `onDragEnd` in this environment despite several rounds of fixes, and the in-session browser tool couldn't do a real drag either (its screenshot/drag actions require the pane to be displayed, which requires the user to have it open). Still true as of the `board_service.py` extraction and `dndCollision.ts` extraction — neither touched `moveCard` or the collision-refinement logic itself, only where it lives. `dndCollision.test.ts` does now unit-test the refinement logic (gap-between-cards resolves to the nearest card; the active card is excluded from its own column's refinement) directly, without a real drag — that's a lower bar than a full mouse-driven Playwright confirmation, but it's the part of the logic most likely to regress silently, and it's green. If you're touching drag-and-drop again, verify this manually first.

## Running the e2e suite

`npm run test:e2e` builds and runs the real Docker image (see `playwright.config.ts`'s `webServer`), so it needs Docker running. Because `reuseExistingServer: true` is set, a container left over from a previous crashed/failed run (`pm-app-e2e`) will be reused instead of a fresh one — if a run looks like it's seeing stale or duplicated data, run `docker rm -f pm-app-e2e` first. `kanban.spec.ts` locates columns/cards by their rendered title/testid rather than hardcoded ids, since the database assigns its own ids. Note: `page.getByDisplayValue(...)` is a Testing Library method, not a Playwright one — to find a column by its current title, read `column.locator("input").first().inputValue()` instead (see `findColumnByTitle` in `kanban.spec.ts`).
